import { NewNgForDirective } from './new-ng-for.directive';

describe('NewNgForDirective', () => {
  it('should create an instance', () => {
    const directive = new NewNgForDirective();
    expect(directive).toBeTruthy();
  });
});
